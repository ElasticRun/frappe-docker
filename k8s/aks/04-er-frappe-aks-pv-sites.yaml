apiVersion: v1
kind: PersistentVolume
metadata:
  name: er-frappe-sites-pv
spec:
  capacity:
    storage: 5Gi
  accessModes:
    - ReadWriteMany
  storageClassName: er-frappe-sites-sc
  azureFile:
    secretName: er-frappe-aks-share-secret
    shareName: er-frappe-aks-sites-share
    secretNamespace: default
    readOnly: false
  mountOptions:
    - dir_mode=0777
    - file_mode=0777
    - mfsymlinks
